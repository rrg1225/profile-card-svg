{
  "name": "profile-card-svg",
  "version": "1.0.0",
  "description": "Dynamic GitHub profile status card generator",
  "main": "api/card.js",
  "type": "module",
  "scripts": {
    "check": "node --check api/card.js",
    "test": "node --test test/*.test.js",
    "health": "node scripts/project-health.js",
    "ops:check": "node scripts/ops-check.js",
    "ci:local": "npm run health && npm run ops:check && npm test"
  },
  "keywords": [
    "github-profile",
    "svg",
    "serverless",
    "vercel",
    "profile-card"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20"
  }
}
